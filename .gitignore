__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# Per-hypothesis falsification artifacts written by the skill at runtime.
# Anchored to repo root so it does NOT match skills/falsify/ (the skill itself must ship).
/falsify/

# Generated fixtures for the worked examples (regenerated by make_fixtures.py).
examples/*/model_scores.csv
examples/*/oracle.csv
examples/*/predictions.csv

# Personal launch draft — not part of the package.
LINKEDIN_DRAFT.md

# LaTeX build intermediates (the .tex source and built .pdf ARE committed).
paper/*.aux
paper/*.log
paper/*.out
paper/*.synctex.gz
